import Environment from "@/components/Environment";

// Home — light-mode Environment / sustainability page (layout adapted from the
// Apple Environment screen, rebuilt with MRH content). Nav comes from the layout.
export default function HomePage() {
  return <Environment />;
}
